server: 
  port: 9296
spring: 
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/failur5/config-server
          default-label: main #branch of the git repository
          clone-on-start: true